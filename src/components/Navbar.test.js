import React from "react";
import Navbar from "./Navbar";

test("it should render correctly", () => {
  expect;
});

describe("when logged in", () => {
  test("it should not render sign in button", () => {});
  test("it should render worker tab if user type is worker", () => {});
  test("it should render hirer tab if user type is hirer", () => {});
});

describe("when not logged in", () => {
  test("it should render sign in button", () => {});
  test("it should not render worker tab", () => {});
  test("it should not render hirer tab", () => {});
});
